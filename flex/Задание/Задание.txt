Задание 1
Измените css-код в файле style.css, в стилях для .items, с помощью flexbox таким образом, чтобы все элементы items 
располагались по центру горизонтально. Остальной код оставьте без изменений.

Задание 2

Измените css-код в файле style.css, в стилях для .items, с помощью flexbox таким образом, чтобы все элементы items 
располагались по центру и горизонтально и вертикально (внутри блока items). 

Задание 3

Измените css-код в файле style.css, в стилях для .items, с помощью flexbox таким образом, чтобы при изменении размера экрана 
(двигайте ползунок виртуального браузера) элементы item, когда размер браузера становится маленьким, переходили на новую строку и выстраивались в столбик.

Задание 4

Измените css-код в файле style.css, с помощью flexbox таким образом, чтобы красный блок item был в два раза больше чем зеленый и желтый элемент. 

Задание 5

Измените css-код в файле style.css, с помощью flexbox таким образом, чтобы желтый блок item расположился снизу справа в блоке items.
